apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: microservices-demo
  labels:
    app: payment
spec:
  selector:
    app: payment
  ports:
  - port: 8082
    targetPort: 8082
  type: ClusterIP
